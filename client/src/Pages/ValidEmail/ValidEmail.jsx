import styles from "./ValidEmail.module.scss";

const ValidEmail = () => {
    return (
      <section className={`${styles.validemail}`}>
          <h1>Validation Email</h1>
      </section>
    )
}
  
export default ValidEmail