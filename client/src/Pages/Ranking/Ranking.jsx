import styles from "./Ranking.module.scss";

const Ranking = () => {
    return (
      <section className={`${styles.ranking}`}>
          <h1>Classement</h1>
      </section>
    )
}
  
export default Ranking