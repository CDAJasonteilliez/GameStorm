import styles from "./RecupPassword.module.scss";

const RecupPassword = () => {
    return (
      <section className={`${styles.recuppassword}`}>
          <h1>Récupération mot de passe</h1>
      </section>
    )
}
  
export default RecupPassword