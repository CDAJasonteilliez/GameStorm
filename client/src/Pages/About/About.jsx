import styles from "./About.module.scss";

const About = () => {
    return (
      <section className={`${styles.about}`}>
          <h1>À propos</h1>
      </section>
    )
}
  
export default About