import styles from "./Dashboard.module.scss";

const Dashboard = () => {
    return (
      <section className={`${styles.dashboard}`}>
          <h1>Dashboard</h1>
      </section>
    )
}
  
export default Dashboard