import styles from "./Profil.module.scss";

const Profil = () => {
    return (
      <section className={`${styles.profil}`}>
          <h1>Profile</h1>
      </section>
    )
}
  
export default Profil